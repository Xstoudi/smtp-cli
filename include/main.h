/* Function to execute to parse an argument. Returns 0 if successful. */
int parse_opt(int key, char* arg, struct argp_state* state);
/* Main function. Returns 0 if successful. */
int main(int argc, char* argv[]);
